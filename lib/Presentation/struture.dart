

import 'package:flutter/material.dart';

class Struture extends StatefulWidget {
  const Struture({super.key});

  @override
  State<Struture> createState() => _StrutureState();
}

class _StrutureState extends State<Struture> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Công thức'),),
    );
  }
}